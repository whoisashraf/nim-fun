# import strutils,os

# let num1 = parseInt(paramStr(1))
# let num2 = parseInt(paramStr(2))
let 
    num1 = 1
    num2 = 100_000_000

let sumResult = (num2 - num1 + 1) * (num1 + num2) / 2

echo sumResult